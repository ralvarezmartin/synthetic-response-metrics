# Interaction export example

This example shows the minimal `interactions` shape expected from a
generic agent run export.

Run:

```powershell
srm compute examples/interaction_export_example/interaction_export.json --include-pairs
```
